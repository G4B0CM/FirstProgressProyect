@model FirstProgressProyect.Models.StudentCompetence

@{
    ViewData["Title"] = "Create";
}

<h4 class="text-center" style="color: #FFFF">Competencias</h4>
<hr />
<div class="container d-flex justify-content-center align-items-center min-vh-100 ">
    <div class="col-md-6">
        <div class="bg-light p-4 rounded shadow" style="background-color: rgba(23, 50, 88, 0.8);">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="StudentId" class="control-label"></label>
                <select asp-for="StudentId" class ="form-control" asp-items="ViewBag.StudentId"></select>
            </div>
            <div class="form-group">
                <label asp-for="CompetenceId" class="control-label"></label>
                <select asp-for="CompetenceId" class ="form-control" asp-items="ViewBag.CompetenceId"></select>
            </div>
            <div class="form-group">
                <label asp-for="DateOfParticipation" class="control-label"></label>
                <input asp-for="DateOfParticipation" class="form-control" />
                <span asp-validation-for="DateOfParticipation" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
        </div>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
