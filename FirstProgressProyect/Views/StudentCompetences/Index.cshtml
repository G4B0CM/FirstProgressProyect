@model IEnumerable<FirstProgressProyect.Models.StudentCompetence>

@{
    ViewData["Title"] = "Index";
}

<h4 class="text-center" style="color: #FFFF">Información de Competencias </h4>
<hr />
<div style=" background-color: rgba(23,50,88,0.8); min-height: 100vh; padding: 20px"
<p>
     <a asp-action="Create"class="btn btn-primary">Añadir Estudiantes</a>
</p>
<table class="table"style="color: #FFFF">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Student)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Competence)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateOfParticipation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Student.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Competence.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateOfParticipation)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
